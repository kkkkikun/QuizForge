# QuizForge LLM 配置示例
#
# 用法：复制本文件为 .env 并填入真实值（.env 已被 gitignore，切勿提交）
#   cp .env.example .env
#
# 不配置任何 LLM 也能正常使用（纯规则解析 + 标记存疑）。

# ───────── Provider 判定 ─────────
# 不设 LLM_PROVIDER 时按 key 自动判定：
#   设了 ANTHROPIC_API_KEY   → Anthropic Claude
#   设了下列任一 OpenAI 系 key → OpenAI 兼容接口
# 想强制指定：LLM_PROVIDER=openai | anthropic

# ───────── 方案 A：DeepSeek（默认，OpenAI 兼容）─────────
LLM_API_KEY=your-deepseek-key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# ───────── 方案 B：OpenAI ─────────
# LLM_API_KEY=your-openai-key
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini

# ───────── 方案 C：Anthropic Claude ─────────
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-anthropic-key
# LLM_MODEL=claude-haiku-4-5-20251001

# ───────── 自定义中转 / 代理 ─────────
# 任何 OpenAI 兼容端点都可：把 LLM_BASE_URL 换成中转地址、LLM_MODEL 换成其暴露的模型 id 即可。
